# Runtime + dev dependencies for the MTF-SMC XAUUSD backtest (V3).
# Developed and tested on Python 3.13.
numpy>=1.24        # core arrays, Monte-Carlo / bootstrap resampling
pandas>=2.1        # OHLC frames, tz-aware resample (2.x+ APIs)
scipy>=1.11        # t-tests, distributions for DSR / PSR, bootstrap helpers
matplotlib>=3.6    # figures only (engine has no plotting dependency)
openpyxl>=3.1      # reads HistData .xlsx (read_only + reset_dimensions)
pyyaml>=6.0        # YAML config grids
pytest>=7.4        # test suite
